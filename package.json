{
  "name": "scribble",
  "version": "1.0.0",
  "description": "A drawing app which allows multiple users to draw on the same canvas in real-time.",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"cd server && pnpm dev\" \"cd client && pnpm dev\"",
    "prepare": "husky install"
  },
  "keywords": [],
  "author": "@geo-mena",
  "license": "ISC",
  "devDependencies": {
    "@commitlint/cli": "^18.4.2",
    "@commitlint/config-conventional": "^19.2.2",
    "concurrently": "^9.1.0",
    "husky": "^9.0.11"
  }
}
